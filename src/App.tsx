import { BrowserRouter } from 'react-router-dom';
import Router from './Router';

function App(): any {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
}

export default App;
